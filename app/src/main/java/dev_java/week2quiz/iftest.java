package dev_java.week2quiz;

public class iftest {


 public Boolean testIfB(String str){
  //매개변수==> 함수를 호출할 때 인수로 전달된 값을 함수 내부에서 사용할 수 있게 해줌
  return Boolean.valueOf(str);
  //Boolean.valueOf()메소드는 파라미터로 문자열을 입력받아서 Boolean타입을 리턴함
 } 
}
